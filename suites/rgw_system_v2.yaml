tests:
   - test:
       name: install ceph pre-requisities
       module: install_prereq.py
       abort-on-fail: true

   - test:
      name: ceph ansible
      module: test_ansible.py
      config:
        ansi_config:
            ceph_test: True
            ceph_origin: distro
            ceph_stable_release: luminous
            ceph_repository: rhcs
            osd_scenario: collocated
            osd_auto_discovery: False
            journal_size: 1024
            ceph_stable: True
            ceph_stable_rh_storage: True
            public_network: 172.16.0.0/12
            fetch_directory: ~/fetch
            copy_admin_key: true
            ceph_conf_overrides:
                global:
                  osd_pool_default_pg_num: 64
                  osd_default_pool_size: 2
                  osd_pool_default_pgp_num: 64
                  mon_max_pg_per_osd: 1024
      desc: test cluster setup using ceph-ansible
      destroy-cluster: false
      abort-on-fail: true
#   - test:
#      name: check-ceph-health
#      module: exec.py
#      config:
#            cmd: ceph -s
#            sudo: True
#      desc: Check for ceph health debug info
   - test:
       name: Mbuckets
       desc: test to create "M" no of buckets
       module: rgw_system_v2.py
       config:
           script-name: test_Mbuckets_with_Nobjects.py
           config-file-name: test_Mbuckets.yaml
           timeout: 300
   - test:
       name: Mbuckets_with_Nobjects
       desc: test to create "M" no of buckets and "N" no of objects
       module: rgw_system_v2.py
       config:
           script-name: test_Mbuckets_with_Nobjects.py
           config-file-name: test_Mbuckets_with_Nobjects.yaml
           timeout: 300
   - test:
       name: Mbuckets_with_Nobjects_delete
       desc: test to create "M" no of buckets and "N" no of objects with delete
       module: rgw_system_v2.py
       config:
           script-name: test_Mbuckets_with_Nobjects.py
           config-file-name: test_Mbuckets_with_Nobjects_delete.yaml
           timeout: 300
   - test:
       name: Mbuckets_with_Nobjects_download
       desc: test to create "M" no of buckets and "N" no of objects with download
       module: rgw_system_v2.py
       config:
           script-name: test_Mbuckets_with_Nobjects.py
           config-file-name: test_Mbuckets_with_Nobjects_download.yaml
           timeout: 300
   - test:
       name: test versioning enable
       desc: test to enable versioning
       module: rgw_system_v2.py
       config:
           script-name: test_versioning_with_objects.py
           config-file-name: test_versioning_enable.yaml
           timeout: 300
   - test:
       name: test versioning enable with objects copy
       desc: test to enable versioning objects copy
       module: rgw_system_v2.py
       config:
           script-name: test_versioning_with_objects.py
           config-file-name: test_versioning_objects_copy.yaml
           timeout: 300
   - test:
       name: test versioning enable with objects
       desc: test to enable versioning objects
       module: rgw_system_v2.py
       config:
           script-name: test_versioning_with_objects.py
           config-file-name: test_versioning_objects_enable.yaml
           timeout: 300
   - test:
       name: test versioning suspend with objects
       desc: test to suspend versioning objects
       module: rgw_system_v2.py
       config:
           script-name: test_versioning_with_objects.py
           config-file-name: test_versioning_objects_suspend.yaml
           timeout: 300
   - test:
       name: test versioning with suspend
       desc: test to suspend versioning without objects
       module: rgw_system_v2.py
       config:
           script-name: test_versioning_suspend.py
           config-file-name: test_versioning_objects_suspend.yaml
           timeout: 300