---
# ODF to FDF migration configuration.
# Sets subscription approval to Manual (required for catalog switch).
DEPLOYMENT:
  subscription_plan_approval: "Manual"

UPGRADE:
  # Set to the full pull-spec of the ISF Data Foundation catalog image.
  fdf_registry_image: "cp.stg.icr.io/cp/df/isf-data-foundation-catalog:4.21.9-4"
