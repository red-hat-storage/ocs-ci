---
# This config file is for the upgrade to the next one DS build follownig the
# current installed build. Mainly used for testing upgrades, so you can install
# som older version and then ran upgrade one by one version.
DEPLOYMENT:
  upgrade: true
  upgrade_to_latest: false
