---
# This config file is for the upgrade purpose
DEPLOYMENT:
  upgrade: true
  # In the case you don't want to upgrade to latest one buiild but just
  # one build up you can uncomment following parameter upgrade_to_latest.
  # This option is usefull mainly for testing op upgrade,  so we can install
  # some older version and run upgrade multiple times till reach latest one.
  # upgrade_to_latest: false
