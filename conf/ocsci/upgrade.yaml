---
# This config file is for the upgrade purpose
DEPLOYMENT:
  upgrade: true
  # In the case you need to upgrade to the latest build, the following
  # parameter has to be set. Current builds does not support that yet, see BZ:
  # https://bugzilla.redhat.com/show_bug.cgi?id=1767400
  # upgrade_to_latest: true
