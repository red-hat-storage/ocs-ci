---
# This config file can be used to enforce live must gather image for OCS
REPORTING:
  ocs_must_gather_image: "registry.redhat.io/ocs4/ocs-must-gather-rhel8"
  ocs_must_gather_latest_tag: 'latest'
  overwrite_must_gather_image: false
