---
ENV_DATA:
  ceph_image: 'quay.io/rhceph-dev/rhceph-4.0-rhel-8:latest'
  ceph_csi_image: 'quay.io/rhceph-dev/cephcsi:latest'
  rook_image: 'quay.io/rhceph-dev/rook:latest'
  # When we get some conclusion on another DS CSI sidecar images from ticket:
  # https://jira.coreos.com/browse/RHSTOR-150 we can add those as well.
  # Variables pre-pripared below:
  # rook_csi_registrar_image:
  # rook_csi_provisioner_image:
  # rook_csi_snapshotter_image:
  # rook_csi_attacher_image:
  cluster_namespace: 'rook-ceph'
