---
# Config file for OCP GA 4.3 stable channel

RUN:
  client_version: '4.3-ga'

DEPLOYMENT:
  ocp_url_template: "https://mirror.openshift.com/pub/openshift-v4/clients/ocp/{version}/{file_name}-{os_type}-{version}.tar.gz"
  installer_version: "4.3-ga"
  terraform_version: "0.11.14"
  ocp_channel: "stable"
ENV_DATA:
  vm_template: 'rhcos-4.3.0-x86_64-vmware'
