apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name:
spec:
  storageClassName:
  dataSource:
    name:
    kind: VolumeSnapshot
    apiGroup: snapshot.storage.k8s.io
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage:

