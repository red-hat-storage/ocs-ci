---
apiVersion: v1
kind: Service
metadata:
  name: svc-roks-icsp
  namespace: kube-system
  labels:
    app: roks-icsp
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app: roks-icsp
  type: ClusterIP
