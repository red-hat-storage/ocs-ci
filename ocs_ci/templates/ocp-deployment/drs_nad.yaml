apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: storage
  namespace: clusters-cl-421-b
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "name": "storage",
      "type": "cnv-bridge",
      "bridge": "br-storage",
      "macspoofchk": true
    }
