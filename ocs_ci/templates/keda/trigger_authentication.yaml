apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: prom-auth
  namespace: keda-demo
spec:
  secretTargetRef:
    - parameter: bearerToken
      name: prom-bearer
      key: bearerToken
    - parameter: ca
      name: prom-ca
      key: ca.crt
