SCENARIO_NAME: "PLATFORM_FAILURES"
SCENARIO_DESCRIPTION: "This scenario will test the platform failures"
FAILURES:
  PLATFORM_INSTANCE_FAILURES:
    FAILURE_DURATION: 30
    WORKLOAD:
      - FIO
      - VDBENCH
  PLATFORM_ZONE_FAILURES:
    FAILURE_DURATION: 60
    WORKLOAD:
      - FIO
      - VDBENCH 
  PLATFORM_NETWORK_FAILURES:
    FAILURE_DURATION: 60
    WORKLOAD:
      - FIO
      - VDBENCH
  PLATFORM_DISK_FAILURES:
    FAILURE_DURATION: 30
