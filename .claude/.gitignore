# Issue verification workflow run outputs (logs, issue JSON per run)
workflow/issue_verification_workflow/run_record/*
!workflow/issue_verification_workflow/run_record/.gitkeep

# Shared vector DB local storage (Qdrant data, manifest)
vectorDB/data/*
!vectorDB/data/.gitkeep
