---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ pvc_name | default ('rbd-pvc') }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ pvc_storage | default ('10Gi') }}
  storageClassName: {{ storageclass_name |default ('ocsci-csi-rbd') }}
